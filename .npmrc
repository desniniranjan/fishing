# NPM Configuration for Cloudflare Pages deployment
# Ensures proper handling of optional dependencies

# Install optional dependencies
optional=true

# Use exact versions for better reproducibility
save-exact=false

# Disable package-lock for better compatibility with different platforms
package-lock=true

# Enable legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Audit level
audit-level=moderate
