import AppLayout from "@/components/layout/AppLayout";

const Staff = () => {
  return (
    <AppLayout>
      {/* Workers page content cleared - page is now blank */}
    </AppLayout>
  );
};

export default Staff;
