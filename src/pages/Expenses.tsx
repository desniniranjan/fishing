import AppLayout from "@/components/layout/AppLayout";

const Expenses = () => {
  return (
    <AppLayout>
      {/* Expenses tab content has been wiped out */}
    </AppLayout>
  );
};

export default Expenses;
