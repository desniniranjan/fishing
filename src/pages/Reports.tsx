import AppLayout from "@/components/layout/AppLayout";

const Reports = () => {
  return (
    <AppLayout>
      {/* Reports page content cleared - page is now blank */}
    </AppLayout>
  );
};

export default Reports;
