import AppLayout from "@/components/layout/AppLayout";

const Customers = () => {
  return (
    <AppLayout>
      {/* Contact tab content has been wiped out */}
    </AppLayout>
  );
};

export default Customers;
